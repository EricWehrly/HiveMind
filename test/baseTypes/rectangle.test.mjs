import Rectangle from "./rectangle.mjs";
import { describe, it } from 'node:test';
import assert from "node:assert/strict";


describe('rectangle', () => {

    it('contains point within rect', () => {

        // const map = new BlackoutMap();

        // assert.equal(map.isBlocked(1, 1), false);
    });
});