// Have to define the interface as a class and assert using tests
export default class ControllerInterface {

    setCharacter(character) {

        // if character variable is not of class type character, throw a bad!
    }
}